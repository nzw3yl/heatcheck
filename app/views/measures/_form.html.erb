<%= simple_form_for @measure, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :content %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                measures_path, :class => 'btn' %>
  </div>
<% end %>
