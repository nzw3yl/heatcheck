<%= form_for @partner, :html => {:class => 'navbar-form'} do |f| %>
  <%= f.hidden_field :parent_id %>
  <%= f.text_field :name, :placeholder => "name" %>
  <%= f.text_field :description, :placeholder => "description" %>
  <%= f.submit "Add", :class => 'btn btn-primary' %>
<% end %>

